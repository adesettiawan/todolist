const path = require("path");

module.exports = {
  config: path.resolve(__dirname, "config", "config.json"), // Path ke file konfigurasi Sequelize
  "models-path": path.resolve(__dirname, "models"), // Path ke folder models
  "migrations-path": path.resolve(__dirname, "migrations"), // Path ke folder migrations
  "seeders-path": path.resolve(__dirname, "seeders"), // Path ke folder seeders
};
